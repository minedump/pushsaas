-- Свободный комментарий мерчанта к автоматизации (зачем настроена, что
-- проверялось и т.п.) — не участвует в логике отправки, чисто заметка для
-- себя/команды. Показывается в разделе «Автоматизации» под названием в
-- формах создания/редактирования, а также в отчёте-презентации по
-- автоматизациям (если задан).
alter table public.automations add column if not exists comment text;
