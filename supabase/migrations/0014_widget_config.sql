-- =====================================================================
--  0014 — Кнопка подписки и слайд-виджет: конфигурация внешнего вида.
--
--  Один jsonb под оба виджета (button.js — плавающая кнопка,
--  prompt.js — новый слайд-виджет, имитирующий системный запрос
--  разрешения). Пустой '{}' = поведение как раньше (кнопка включена с
--  дефолтным видом, плашка выключена) — обратная совместимость для уже
--  подключённых сайтов не требует отдельной миграции данных.
-- =====================================================================

alter table public.projects add column if not exists widget_config jsonb not null default '{}'::jsonb;
