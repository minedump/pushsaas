-- Каждое приветственное сообщение само решает, участвует ли оно в
-- приоритезации каналов (см. 0045/0046) — по умолчанию да (учитывает
-- приоритет, может быть подавлено более приоритетным активным каналом),
-- но можно явно поставить «отправлять всегда», игнорируя приоритет.
alter table public.automations add column if not exists respects_priority boolean not null default true;
