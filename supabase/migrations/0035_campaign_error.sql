-- Причина, по которой рассылка не смогла стартовать (нет провайдера/ключей,
-- недостаточно баланса, нет обязательной ссылки отписки) — раньше терялась
-- сразу после ответа API/крона, в «Журнале» (вкладка «Ошибки») строка была
-- без объяснения. Пишется рядом со status='failed' во всех местах, где он
-- выставляется (lib/sender.ts, send-scheduled, send-draft).
alter table public.campaigns add column if not exists error text;
