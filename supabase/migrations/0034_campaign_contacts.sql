-- Адресная отправка (поле «Контакты») раньше не переживала черновик/
-- планирование — некуда было её сохранить, поэтому черновик с контактами
-- отклонялся, а запланированный push с контактами отправлялся немедленно.
-- Теперь сырые контакты (телефоны и/или email вперемешку — кросс-канальное
-- сопоставление резолвится при реальной отправке, см. lib/identity.ts)
-- хранятся на самой кампании и резолвятся заново в момент фактической
-- отправки (send-draft, крон send-scheduled), как и сегмент.
alter table public.campaigns add column if not exists contacts text[] default '{}'::text[];
