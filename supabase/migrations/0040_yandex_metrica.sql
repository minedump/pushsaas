-- Номер счётчика Яндекс.Метрики мерчанта (настраивается в «Настройки») —
-- виджет использует его, чтобы вызвать ym(counterId, 'getClientID', cb)
-- максимально точно (а не гадать по _ym_uid-куке, которая не привязана к
-- конкретному счётчику, если на странице их несколько).
alter table public.projects add column if not exists ym_counter_id text;
