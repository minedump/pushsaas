-- Контекстный JSON шаблона (та же идея, что «Ручной JSON-контекст» у
-- рассылок, ContextField.tsx) — раньше был только для превью в форме
-- редактирования шаблона, никуда не сохранялся. Теперь это дефолтные
-- Liquid-данные шаблона: подставляются при КАЖДОЙ отправке этим шаблоном
-- (приветственные автоматизации — lib/sender.sendWelcomeNow), реальные
-- данные контакта/разового вызова их перекрывают при совпадении ключа.
alter table public.templates add column if not exists context jsonb;
