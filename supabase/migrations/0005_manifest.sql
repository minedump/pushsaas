-- =====================================================================
--  0005 — PWA-манифест проекта
--  Конфигурация webmanifest (название, цвет темы, ссылки на иконки,
--  сгенерированные из загруженного клиентом логотипа) хранится на проекте.
--  Сами иконки — в Supabase Storage (bucket project-assets, публичный).
-- =====================================================================

alter table public.projects
  add column if not exists manifest_config jsonb;
