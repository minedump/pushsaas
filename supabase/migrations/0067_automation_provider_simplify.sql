-- Провайдер конкретной автоматизации (не-каскадный режим) — раньше единый
-- канал+провайдер брался только из проектной настройки «Приоритет каналов»;
-- теперь она отвечает только за каскадный режим (см. resolveCascadeChannel),
-- а обычная (не-каскадная) карточка может выбрать провайдер сама, если у
-- канала их несколько подключено.
alter table public.automations add column if not exists provider text;
