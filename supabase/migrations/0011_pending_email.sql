-- =====================================================================
--  Временное хранение email, введённого вручную ДО того как телефон
--  подтверждён (сценарии: каскад кодов исчерпан и мы просим почту как
--  последний рубеж; либо вход стартует с почты, она не найдена — просим
--  телефон, помня почту). На verify — сливается в identities.email, если
--  там ещё пусто.
-- =====================================================================

alter table public.oidc_auth_sessions add column if not exists pending_email text;
