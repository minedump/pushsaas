-- =====================================================================
--  0021 — Явный ID транзакционного канала Haskimail (MessageStream), в
--  дополнение к рассылочному (0020). Без него письма шли в дефолтный
--  канал "outbound" — это по-прежнему поведение по умолчанию, если поле
--  не заполнено; явный ID нужен, когда у аккаунта своя нумерация каналов
--  (см. HASKIMAIL_TRANSACTIONAL_STREAM в кабинете Haskimail).
-- =====================================================================

alter table public.project_secrets add column if not exists haskimail_transactional_stream text;
